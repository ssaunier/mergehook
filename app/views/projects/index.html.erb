<table>
  <%= @projects.each do |project| %>
  <tr>
    <td><%= project.repo %></td>
    <td>
      <%= link_to "Delete", project_path(project), method: :delete, confirm: "Are you sure?" %>
    </td>
  </tr>
  <% end %>
</table>